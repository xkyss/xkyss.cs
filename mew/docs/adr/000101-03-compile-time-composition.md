# 编译期组合,无 DI、无插件加载

Workbench 面向个人自用,追求简单、可剪裁与 AOT 友好。决定:采用**编译期组合**——用类型安全 Fluent API 在代码中组装工作台,不引入 IoC 容器,也不做运行时插件加载。

## Considered Options

- **运行时插件 + DI**:扩展性最强,但引入反射与动态加载,损害 AOT/Trim 兼容和调试体验。
- **编译期组合(选定)**:扩展需要改代码重新编译,换来可剪裁、可 AOT、依赖显式、调试简单。

## Consequences

- 后续扩展(新视图、新工具)以编译期注册的方式接入。
