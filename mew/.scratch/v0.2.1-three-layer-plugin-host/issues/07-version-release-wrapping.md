# 07 — 版本收尾与双 exe 发布

**What to build:** 完成版本号提升、术语与文档同步、双 exe 发布与双路径冒烟，确保 v0.2.1 可分发。

**Blocked by:** 01 — 插件清单模型与发现（含校验与启用态）, 02 — 扩展主机进程与五区职责切分, 03 — IPC 契约与跨进程搜索聚合, 04 — DLL 运行时加载（ALC 隔离）, 05 — 热键与设置节的跨进程代理, 06 — 故障隔离与可观测性

**Status:** resolved

- [ ] 宿主内 `AppVersion` 单行提升至 `v0.2.1`（单文件单行改动，先例 `ba2885e`）
- [ ] `CONTEXT.md` 同步三层术语（工具模块保留指代富插件、独立插件指代进程外插件、扩展主机指代 Layer 2、插件统一指代运行时插件）与呼出浮层为框架级机制的描述
- [ ] CI 产出双 exe（`Mew.Host.exe` AOT + `Mew.PluginHost.exe` JIT）同目录分发，单 AOT 回退路径可验证（DLL 插件置灰）
- [ ] 双路径手工冒烟：浮层呼出与跨源搜索、托盘常驻、全局热键、设置读写与插件启用/禁用/崩溃恢复均符合预期
