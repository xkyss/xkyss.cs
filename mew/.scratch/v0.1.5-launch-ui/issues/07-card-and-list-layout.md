# 07 — 卡片与列表布局

**What to build:** 卡片改为固定约 170×96 尺寸，24px 图标+名称同行、命令小字第二行；列表行保持图标+名称/命令两行排布，行尾为悬停「编辑」按钮；悬停/选中视觉与 05 的状态一致。

**Blocked by:** 05 — 悬停/选中视觉与悬停编辑按钮.

**Status:** resolved

- [x] 卡片 ~170×96 固定，24px 图标与名称同行，命令小字第二行，名称过长可换行。
- [x] 列表行信息排布与行尾悬停「编辑」按钮位置正确。
- [x] 卡片/列表形态切换、搜索过滤、新增后的刷新与现有一致（无回归）。
- [x] 手工冒烟覆盖整体启动上下文（与 01-06 成果同窗验收）。

## Comments

- 实现提交 `8fc549f` feat(launcher): 卡片布局重设计(图标名称同行、命令小字、加宽至 170)。
- 卡片 = `WrapPanel` 格宽 170×96；内容改为「24px 图标 + 名称同行（`Grid Auto,*` 星列占剩余宽度可换行）+ 命令 11px 第二行」；列表行排布与行尾悬停「编辑」由 01/04/05 落地，本票为验收项。
- 纯布局改动，无纯逻辑测试点；整体 61/61 回归通过（形态切换/搜索过滤/新增刷新走 `ShowNav` 统一路径，未改动）。
- 待手工冒烟：卡片信息排布与换行、列表行排布、形态切换/搜索/新增无回归、与 01-06 全部成果同窗验收。
