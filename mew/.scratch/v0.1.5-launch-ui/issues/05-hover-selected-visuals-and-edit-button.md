# 05 — 悬停/选中视觉与悬停编辑按钮

**What to build:** 列表项（卡片/列表）悬停时背景微亮；键盘选中项以 accent 背景+左缘条高亮，与悬停态可区分；悬停时浮现「编辑」按钮（卡片角落、列表行尾），点击打开详情编辑。

**Blocked by:** 04 — 键盘导航与选中模型.

**Status:** resolved

- [x] 悬停态背景微亮，颜色取自五区色板。
- [x] 选中态 accent 高亮（背景+左缘条），与悬停态视觉可区分。
- [x] 悬停时卡片角落与列表行尾浮现「编辑」按钮，点击进入详情编辑。
- [x] 亮/暗主题下状态颜色均适配。

## Comments

- 实现提交 `ecc32f0` feat(launcher): 列表悬停/选中视觉与悬停编辑按钮。
- 三态（选中 > 悬停 > 常态）收敛在 `StyleListItem`；悬停计数抽为 `HoverRefCount`（主按钮与编辑按钮共用，对事件顺序免疫），`HoverRefCountTests` 6 例全过；整体 60/60 通过。
- 选中 = accent 背景 + 3px 左缘条（独立 Border 元素，`WithTheme` 主题色）；悬停 = 背景微亮（暗主题向白 8%、亮主题向黑 6%）；「编辑」按钮与主按钮兄弟叠加（不嵌套避免点击冲突）。
- 待手工冒烟：悬停微亮与「编辑」按钮浮现（卡片右上角/行尾）、点「编辑」开详情且不触发启动、选中左缘条与悬停可区分、选中项上悬停保持 accent、亮/暗主题下各态颜色。
