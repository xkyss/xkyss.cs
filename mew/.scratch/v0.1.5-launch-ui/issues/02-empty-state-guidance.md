# 02 — 空状态引导

**What to build:** 空状态分场景引导：「暂无启动项」显示图标+文案+「＋新增启动项」按钮；「无匹配启动项」「无匹配分类」显示图标+文案+「清空搜索」按钮；分类树侧边栏与启动项列表共用同一套空状态组件。

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] 「暂无启动项」提供「＋新增启动项」按钮，点击可新增。
- [x] 「无匹配启动项」「无匹配分类」提供「清空搜索」按钮，点击清空筛选恢复内容。
- [x] 三种空态与动作判定抽为纯逻辑并在现有测试项目补用例。
- [x] 空状态组件颜色取自五区色板，亮/暗主题下均适配。

## Comments

- 实现提交 `7d3ddc5` feat(launcher): 空状态分场景引导(暂无/无匹配)与动作按钮。
- 判定抽为 `EmptyState.ForList/ForCategoryTree`（分类树空态恒为无匹配——树恒有「全部/未分类」固定节点），`EmptyStateTests` 5 例全过；整体 43/43 通过。
- 空态图标未做：MewUI GlyphKind 仅 13 字形、无搜索/信息类，属框架约束（验收项未列图标）。
- 待手工冒烟：三处空态的出现与动作按钮、亮/暗主题下颜色。
