# 01 — 列表单击启动与双击防重

**What to build:** 启动项列表（卡片/列表两形态）单击即启动该启动项；双击同一启动项不会重复启动（防重时间窗）；右键菜单「编辑/删除」保持可用。

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] 卡片与列表行单击即启动对应启动项。
- [x] 双击同一启动项只启动一次（防重时间窗生效，不连开两次）。
- [x] 右键菜单「编辑/删除」行为与现有一致。
- [x] 防重判定抽为纯逻辑并在现有测试项目补用例（时间窗边界、同项/异项、恢复）。

## Comments

- 实现提交 `8deafee` feat(launcher): 启动项列表单击启动并防双击重复。
- 防重逻辑抽为 `LaunchDebouncer`（500ms 时间窗，与 Windows 双击时长一致），`LaunchDebouncerTests` 5 例全过；整体 38/38 通过。
- 行尾「启动」按钮按 spec/ADR `000105-01` 移除，悬停「编辑」按钮归属 05。
- 待手工冒烟：卡片/列表单击启动、双击防重、慢速再点恢复、右键编辑/删除。
