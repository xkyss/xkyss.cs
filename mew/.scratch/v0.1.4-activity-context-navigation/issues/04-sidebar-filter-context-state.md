# 04 — 保留侧边栏筛选的上下文状态

**What to build:** 让每个活动栏上下文在本次运行中保留自己的侧边栏筛选条件。用户从一个上下文切换到另一个再返回时看到先前的筛选；重启 Launcher 后从无筛选的干净状态开始。

**Blocked by:** 01 — 活动栏上下文的基础导航.

**Status:** ready-for-agent

- [ ] 每个活动栏上下文的侧边栏筛选独立保存于运行期状态。
- [ ] 同次运行中切换活动栏上下文再返回时，筛选条件及其可见结果仍一致。
- [ ] 重启后不恢复任何侧边栏筛选条件。
