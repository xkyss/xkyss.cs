# v0.1.3 验收记录

> 依据 `.scratch/v0.1.3-launcher-rework/spec.md` 的 8 项手工冒烟清单与七张票据验收项。
> 状态:🟢 已自动化验证 / 🔲 待人工冒烟 / ⚠️ 已知上游缺陷(ADR 000102-01)

## 一、已自动化验证(本环境可执行部分)

| # | 验收项 | 结果 | 方式 |
|---|---|---|---|
| 06 | 数据迁移(旧平铺数组 → 分类树结构) | 🟢 | `Mew.Launcher.Tests` 31 用例全绿:迁移(中文分类 / 「默认」→未分类 / 空列表 / 缺字段归一 / 迁移写回)、子树聚合(含子孙)、「全部」= 所有分类 + 未分类、悬空引用归「未分类」、手写新格式 JSON 识别 |
| — | 分类树纯逻辑(搜索/增删改/展平) | 🟢 | `FilterNavTree`(命中保留子树+父链、固定节点隐藏)、`RemoveCategoryNode` 连根删、`AddCategoryNode`、`RenameCategoryNode`、`FlattenCategoryOptions`(未分类置顶 + 父/子路径)用例覆盖 |
| — | 设置往返(形态偏好) | 🟢 | `SettingsStore` ItemsViewMode 等字段保存/加载/手写 JSON 用例 |
| — | 浮层 / 每项热键 / 托盘 / 搜索代码回归 | 🟢 | grep 验证:回归路径无已删 `LauncherItem.Category` 残留引用(票据 03 contract 完成) |
| 08 | NativeAOT 单文件发布 | 🟢 | `dotnet publish -r win-x64 -p:PublishSingleFile=true -p:PublishAot=true` 成功;单文件 **6.5MB**;exe 可启动(进程常驻至超时终止,未崩溃) |

## 二、待人工冒烟清单(逐项操作 → 预期)

### 01 活动栏与上下文切换
- [ ] 🔲 活动栏仅「启动」「设置」两图标,设置在最底部;「启动」切到分类树 + 启动项列表,「设置」切到设置分类 + 设置内容
- [ ] 🔲 在「启动」上下文选中某分类、打开某详情 → 切「设置」再切回 → 选中分类、搜索词、打开的详情标签仍在
- [ ] 🔲 在「设置」上下文选中「热键」分类 → 切走再回 → 仍显示「热键」内容

### 02 分类树
- [ ] 🔲 多级分类展开/折叠正常;选中分类 → 编辑器区显示该分类(含子孙)所有启动项
- [ ] 🔲 侧边栏顶部分类搜索:输入命中(如「游」)只剩匹配分类及其父链,清空恢复完整树(含「全部」「未分类」)
- [ ] 🔲 分类节点右键:新建子分类 / 重命名 / 删除;「全部」「未分类」右键无菜单
- [ ] 🔲 新建/重命名输入对话框确定/取消正常,空白名不生效
- [ ] 🔲 删除分类弹确认框并提示将删除的启动项数量;确认后该分类(含子分类)与其启动项全部消失;若当前正浏览被删分类,回退「全部」

### 03 编辑器区(启动项列表)
- [ ] 🔲 默认卡片形态(大图标 + 名称网格),工具栏「列表」按钮切换为行列表(图标 + 名称 + 命令 + 行尾「启动」按钮)
- [ ] 🔲 卡片/列表形态重启后保留
- [ ] 🔲 单击卡片/行 → 打开「启动项详情」;双击 → 启动;右键菜单「编辑 / 删除」可用(注意:双击启动时若先触发单击开详情,记录现象)
- [ ] 🔲 列表顶部搜索过滤当前分类(聚合视图)下的启动项;无匹配显示「无匹配启动项」
- [ ] 🔲 「＋ 新增」:在分类下新建 → 详情打开且分类选择器显示该分类;在「全部」「未分类」下新建 → 归未分类
- [ ] 🔲 空分类显示「暂无启动项」

### 04 详情表单
- [ ] 🔲 「分类」字段为下拉选择器:未分类置顶,子分类显示「父 / 子」路径;改选后列表/树同步
- [ ] 🔲 其余字段(名称/命令/参数/工作目录/图标/每项热键)编辑即时保存;「启动」「删除启动项」按钮可用

### 05 设置上下文
- [ ] 🔲 侧边栏「外观 / 热键 / 数据」三分类,编辑器区显示对应内容
- [ ] 🔲 外观:主题三选一即时生效并持久化,状态栏循环按钮与设置一致
- [ ] 🔲 热键:点「更改」按新组合键 → 提示「已生效」、状态栏文案更新;新键立即生效、旧键失效;无效组合/与每项热键冲突有提示
- [ ] 🔲 数据:显示 launcher.json 完整路径,「打开所在文件夹」在资源管理器中选中该文件
- [ ] 🔲 标题栏齿轮与 File→设置 切到「设置」上下文(同一入口)

### 06 数据迁移(真实首启)
- [ ] 🔲 若 `%APPDATA%\Mew\launcher.json` 为旧格式(数组):启动后自动转为新结构(分类树 + 引用),启动项与分类不丢,「默认」→ 未分类;文件已写回新格式
- [ ] 🔲 手写新格式 JSON 可被识别;悬空 categoryId 项归「未分类」

### 07 回归(浮层 / 每项热键 / 托盘 / 日志 / 状态栏)
- [ ] 🔲 全局热键呼出浮层:输入过滤、回车启动、Esc 关闭;与窗口内数据一致
- [ ] 🔲 每项热键绑定/生效;关闭主窗口 → 托盘常驻,热键仍可用;托盘「显示主窗口」「退出」正常
- [ ] 🔲 启动项启动 → 底部面板日志追加、状态栏更新最近结果与呼出热键

### 08 NativeAOT
- [ ] 🟢 单文件发布通过(见「已自动化验证」);建议实测双击启动耗时接近瞬时

## 三、已知上游缺陷(非本版本缺陷,ADR 000102-01)

- **运行时主题切换局部渲染损坏**:MewUI 0.19.1,普通窗口亦可复现;重启恢复;升级需先评审(ADR 000101-01)。本版本主题设置交互(设置上下文外观分类)同样受影响,重启可恢复。
