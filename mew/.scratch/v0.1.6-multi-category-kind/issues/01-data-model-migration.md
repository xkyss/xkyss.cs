# 01 — 多分类数据模型与迁移

**What to build:** 启动项的归属从单值分类改为分类数组:每个启动项可挂多个分类(空数组 = 未分类),旧数据自动迁移,数据层的聚合与归一全部按数组语义工作,UI 组织行为与迁移前保持一致(单选行为不变)。

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] 旧 `launcher.json`(单值归属)加载后自动迁移为数组,启动项归属不变;原「未分类」项迁移后仍归「未分类」
- [x] 手写 JSON 缺省归属字段的启动项被归一为空归属,归「未分类」
- [x] 数据层聚合语义保持:选中父分类显示其子树(含子孙)全部项;「全部」显示所有项;「未分类」只显示空归属项
- [x] 悬空引用(归属指向不存在的分类)的启动项被归一为「未分类」
- [x] 保存后数据文件读写往返一致
- [x] 数据层与存储的既有测试更新后全绿,UI 侧边栏/列表/详情显示与迁移前一致

## Comments

- 悬空引用按「逐 id 剔除」实现:数组模型下仅移除不存在的 id,全部悬空才归「未分类」;部分悬空(如 `["games","ghost"]`)保留有效归属——整项打成未分类会丢数据。
- 「删除分类连根删」对多归属项的误删风险属 ticket 02(删除 = 摘除归属)的语义变更范围,不在本票处理。
