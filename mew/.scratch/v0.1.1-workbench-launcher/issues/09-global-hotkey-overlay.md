# 09 — 全局热键呼出浮层

**What to build:** 任意窗口之上,按默认全局热键 Ctrl+Alt+Space 呼出浮层:输入即过滤(复用侧边栏同一过滤逻辑)、回车启动选中项、Esc 关闭;浮层与窗口内管理共用同一份启动项数据。

**Blocked by:** 04 — 启动项模型与 JSON 数据源 + 侧边栏列表、06 — 搜索过滤

**Status:** ready-for-agent

- [x] 默认全局热键 Ctrl+Alt+Space 呼出浮层
- [x] 浮层输入即过滤(复用 06 的过滤逻辑),回车启动,Esc 关闭
- [x] 浮层与窗口内管理共用同一份启动项数据
