# Mew

个人自用的桌面 UI 框架仓库：在 MewUI 之上构建可复用的 Workbench（工作台）框架库与示例工具 App，UI 布局参考 VSCode，技术栈以 .NET 10 为主。

## Language

**Workbench**:
基于 MewUI 构建的可复用桌面应用外壳框架（即本仓库的「主框架」），提供 VSCode 式布局（活动栏、侧边栏、状态栏、中央编辑器区、底部面板等）与主题/布局能力；五区由扩展主机承载，托盘、全局热键、呼出浮层框架与设置持久化等系统级服务由宿主常驻承载，二者经 IPC 协同。
_Avoid_: framework、shell、UI 框架

**MewUI**:
底层第三方 UI 框架（Aprillz.MewUI），提供控件、样式与跨平台渲染后端。本仓库在其之上构建，不 fork、不修改其源码。
_Avoid_: 底层框架、底层库

**示例工具 App**:
与 Workbench 同仓、以工具模块形式注册进宿主的实际应用，是框架的验证载体。本仓库的第一个工具模块是**应用启动管理器（Launcher）**。
_Avoid_: demo、sample

**工具模块(Tool Module)**:
可操作 Workbench 五区的富插件工程：`T1 编译期工具模块`（源码引用、随宿主 AOT 发布）与 `T2 运行期 DLL 插件`（`AssemblyLoadContext` 隔离加载、需 JIT 扩展主机）共享同一份 Workbench 主框架源码（不携带框架副本），通过 `IMewToolModule` 贡献契约接入五区与外壳服务，可替换、单独演进。Launcher 是第一个工具模块（T1）。
_Avoid_: demo、sample

**应用启动管理器（Launcher）**:
本仓库的示例工具 App：管理并快速启动程序、脚本等启动项，支持搜索过滤与全局热键呼出。
_Avoid_: 启动器、app launcher、快捷启动器

**启动项(Launcher Item)**:
应用启动管理器管理的单个可启动条目(程序、脚本、URL 等),包含名称与启动命令,并归属分类树中的若干分类节点(可多挂,未挂任何分类 = 未分类);是 Launcher 的数据核心。
_Avoid_: item、条目(文档中用「启动项」)

**分类(Category)**:
Launcher 中组织启动项的多级树形分组:树中每个节点有唯一 slug id 与显示名称,可含子分类;启动项通过 id 归属一个或多个分类节点(多挂),移动/重命名/删除分类不影响启动项数据本身。
_Avoid_: 分组、标签、文件夹(文档中用「分类」)

**未分类(Uncategorized)**:
分类树的固定节点(树底收尾,不可管理):聚合所有未挂任何分类节点(归属列表为空)的启动项,「全部」视图包含它;旧模型的「默认」分类迁移后归入此处。
_Avoid_: 默认、无分类(「默认」是旧模型遗留)

**启动类型(Item Kind)**:
从启动命令推导的启动项种类(URL / 程序),不落盘存储,仅用于列表徽标与类型过滤等展示与筛选;命令是唯一事实来源,推导结果与命令恒一致。
_Avoid_: 类型标签、条目类型、kind(文档中用「启动类型」)

**呼出浮层（Overlay）**:
宿主(AOT 常驻)通过全局热键呼出的快捷搜索悬浮窗口，独立于 Workbench 主窗口；框架提供浮层机制与跨源结果混排，工具模块/独立插件贡献搜索源与结果处理，Launcher 的启动项搜索即其搜索源之一，多源结果扁平混排并带来源标记，唯一源时与单源时代一致。
_Avoid_: 弹窗、悬浮窗、popup

**独立插件(Independent Plugin)**:
进程外独立可双击运行的工具（`T3`），以 `--mew-plugin` 常驻态经 `plugin.json` 清单 + `JSON-RPC over NamedPipe` 与宿主对话，仅提供声明式能力（搜索、设置节、热键），不直接操作五区视觉树；崩溃仅标为已崩溃，不影响宿主常驻能力。
_Avoid_: 插件进程、外置工具

**扩展主机(Plugin Host)**:
承载 Workbench 五区（活动栏、侧边栏、编辑器区、底部面板、状态栏）与 `T1/T2` 工具模块的 JIT 子进程（`Mew.PluginHost.exe`）；五区本身视为第一个内部插件，与外部插件同等注册路径；崩溃不影响宿主的托盘/热键/浮层，宿主捕获后自动拉起。
_Avoid_: 插件宿主、子进程（口语可用，文档中用扩展主机）

**插件(Plugin)**:
运行时插件的统称，指 `T2 运行期 DLL 插件` 与 `T3 独立插件` 的并集，与 `工具模块`（可操作五区的富插件）并列；均需满足 `plugin.json` 清单与 `protocolVersion` 契约。
_Avoid_: 扩展、extension（文档中用插件）

**工具行（Toolbar Row）**:
Launcher 各视图第一行的操作区，承载该视图的主要即时操作：侧边栏第一行为分类搜索框，启动项列表第一行为搜索+新增+形态切换，详情页第一行为启动/删除按钮。视图其余内容位于工具行之下。
_Avoid_: 工具栏、顶部操作区

**活动栏（Activity Bar）**:
Workbench 左侧的纵向图标栏，用于切换侧边栏显示的面板；每一项都必须与唯一侧边栏视图配对，不承载独立动作。
_Avoid_: icon bar、导航栏

**活动栏上下文（Activity Context）**:
由当前选中的活动栏项及其唯一对应的侧边栏视图构成的工作台导航状态，活动栏以持续选中样式表示该状态；收起侧边栏不会清除该状态，再次选择当前项会恢复其视图，选择任一项会显示其对应视图。工作台会恢复该状态及侧边栏显隐；已失效的当前项回退到第一个已注册项。
_Avoid_: 当前页面、选中图标

**侧边栏（Side Bar）**:
Workbench 左侧可停靠、可隐藏的辅助面板区域，承载具体工具视图（如资源管理器、搜索）。
_Avoid_: sidebar、面板（与底部面板区分）

**侧边栏筛选（Side Bar Filter）**:
某个活动栏上下文内用于缩小侧边栏内容范围的临时条件；同次运行中切换上下文后会保留，工作台重启后不恢复。
_Avoid_: 全局搜索、持久化筛选

**编辑器区（Editor Area）**:
Workbench 中央的标签化文档区域，支持停靠与拆分，基于 MewDock 的 tabset；其已打开文档不随活动栏上下文切换而改变，关闭文档也不改变侧边栏选择或活动栏上下文，且切换标签不自动反向改变活动栏上下文；侧边栏中同一可编辑对象只对应一个可激活文档。
_Avoid_: 编辑区、中央区域

**在侧边栏定位当前文档（Reveal Active Document）**:
由用户显式触发、可从编辑器标签右键菜单发现且允许应用绑定快捷键的反向导航命令：切换至当前文档所属的活动栏上下文、展开侧边栏，并选中对应对象；无所属活动栏上下文的文档不提供此命令。
_Avoid_: 自动同步、跟随当前标签

**底部面板（Panel）**:
Workbench 底部的可停靠、可折叠面板区域，用于输出、终端等辅助内容。
_Avoid_: bottom panel、底栏（与状态栏区分）

**状态栏（Status Bar）**:
Workbench 底部横条，展示当前上下文信息与操作入口。
_Avoid_: statusbar、底栏

**主题（Theme）**:
Workbench 的亮/暗外观体系，默认跟随系统并可手动切换；由 MewUI 的 ThemeManager（Seed/Metrics/Accent）与 Workbench 五区色板共同决定。
_Avoid_: 皮肤、配色方案

**五区色板（Zone Palette）**:
Workbench 定义的五区专用颜色集合（活动栏、侧边栏、编辑器区、底部面板、状态栏各自的背景与文字色），工具通过 Workbench 主题上下文读取。
_Avoid_: palette（口语可用，文档中用「五区色板」）

**标题栏（Title Bar）**:
Workbench 外壳顶部的自绘标题栏，承载应用图标、菜单栏、居中标题与右侧操作入口（含窗口按钮），替代系统标题栏；背景跟随窗口背景，不属五区色板。
_Avoid_: titlebar、系统标题栏、窗口栏

**菜单栏（Menu Bar）**:
标题栏左区内的一行菜单（如 File、Help），以访问键与快捷键触发命令。
_Avoid_: menubar、menu bar、菜单条
